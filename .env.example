# Client-side env vars must be prefixed with VITE_

# Optional read-only token for DCS (if required)
VITE_DCS_READ_ONLY_TOKEN=

# S3 bucket name used by the preview service
VITE_PREVIEW_S3_BUCKET_NAME=dev-preview.door43.org

# Client/server verification key (must match server if enforced)
VITE_PREVIEW_VERIFICATION_KEY=CHANGEME_MATCHES_SERVER
