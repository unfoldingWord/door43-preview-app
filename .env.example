# Server-side configuration (used by Express server at runtime)
# These are provided to the React client via /api/config endpoint

PORT=3001
CACHE_DIR=./cached-files

# DCS API Token (Optional)
# Purpose: Allows access to public DCS repos that require authentication
# How to get:
#   1. Create a dummy DCS user at https://qa.door43.org (or your DCS instance)
#   2. Go to Settings > Applications > Generate New Token
#   3. Select "public_repo" scope (read-only access to public repositories)
#   4. Copy the generated token here
# Leave empty if all resources are publicly accessible without authentication
DCS_READ_ONLY_TOKEN=

# Preview Verification Key (Recommended)
# Purpose: Shared secret between client and server to validate cache upload requests
#          Client sends this key; server validates before accepting cached content
#          Prevents unauthorized users from uploading arbitrary data
# How to generate: openssl rand -hex 32
# Or use any random string of your choice
PREVIEW_VERIFICATION_KEY=your_secret_key_here
